/* for (each question retrieved from the database) {
     if (question.qType === "TR") {
        //display the question text
        // display a text box that allows the user to respond
        // #qDiv is the container we all each question to
        $(#qdiv).append("<textArea name="TRquestionNumResponse>");
     } else if (question.qType === "SS") {
        // display the questions text
        // create a button group with radio buttons for each option so that only 1 can be chosen
     } else { // question is a multiple selection
        // display the question text
        // display all of the options with checkboxes and allow multiple to be checked
     }
  }
     
     // when the page is submitted, an array should grab each question's value and send it to the database
*/